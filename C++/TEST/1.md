```mermaid
classDiagram
    class TrafficLight {
        <<abstract>>
        +String state
        +TrafficLight traffLight
        +Context cxt
        +String RED
        +String YELLOW
        +String GREEN
        +String BLACK
        +getCurrentState()
        +setupContext(Context)
        +performTask()*
        +setColor()*
        +changeState()*
    }

    class Context {
        -TrafficLight light
        -TrafficLight redLight
        -TrafficLight greenLight
        -TrafficLight yellowLight
        -TrafficLight blackLight
        -String state
        +getLightState()
        +getColor()
        +doAction()
        +setState(String)
        +setupStateObj()
    }

    class RedLight {
        +RedLight()
        +setColor()
        +performTask()
        +changeState()
    }

    class GreenLight {
        +GreenLight()
        +setColor()
        +performTask()
        +changeState()
    }

    class YellowLight {
        +YellowLight()
        +setColor()
        +performTask()
        +changeState()
    }

    class BlackLight {
        +BlackLight()
        +setColor()
        +performTask()
        +changeState()
        +changeState()
    }

    class Surveillance {
        +takePictures()
        +doStatistics()
        +recordViolations()
    }

    TrafficLight <|-- RedLight
    TrafficLight <|-- GreenLight
    TrafficLight <|-- YellowLight
    TrafficLight <|-- BlackLight
    Context --> TrafficLight
    RedLight --> Surveillance
    GreenLight --> Surveillance
    YellowLight --> Surveillance
    BlackLight --> Surveillance
    ```